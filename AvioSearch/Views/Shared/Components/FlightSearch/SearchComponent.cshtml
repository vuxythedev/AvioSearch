<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flight Search</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 300px;
        }

        .input-group {
            margin-bottom: 15px;
        }

        .input-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .input-group input {
            width: 100%;
            padding: 8px;
            border-radius: 3px;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }

        .btn {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 3px;
            padding: 10px 20px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h3>Flight Search</h3>

        <div class="input-group">
            <label for="originAirport">Origin Airport:</label>
            <input id="originAirport" type="text" placeholder="Enter origin airport" />
        </div>

        <div class="input-group">
            <label for="destinationAirport">Destination Airport:</label>
            <input id="destinationAirport" type="text" placeholder="Enter destination airport" />
        </div>

        <div class="input-group">
            <label for="departureDate">Departure Date:</label>
            <input id="departureDate" type="date" />
        </div>

        <div class="input-group">
            <label for="passengerCount">Passenger Count:</label>
            <input id="passengerCount" type="number" min="1" />
        </div>

        <button class="btn" onclick="searchFlights()">Search Flights</button>
    </div>

    <script>
        function searchFlights() {
            var originAirport = document.getElementById("originAirport").value;
            var destinationAirport = document.getElementById("destinationAirport").value;
            var departureDate = document.getElementById("departureDate").value;
            var passengerCount = document.getElementById("passengerCount").value;

            // Here you can perform further operations, like making an API call to fetch flight results
            // Example:
            // fetch('api/flights/search', {
            //     method: 'POST',
            //     body: JSON.stringify({ originAirport, destinationAirport, departureDate, passengerCount }),
            //     headers: {
            //         'Content-Type': 'application/json'
            //     }
            // })
            // .then(response => response.json())
            // .then(data => {
            //     // Handle the flight search results
            //     // Display results in a table or any other format
            // })
            // .catch(error => console.error('Error:', error));
        }
    </script>
</body>
</html>